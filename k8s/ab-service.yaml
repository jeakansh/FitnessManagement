apiVersion: v1
kind: Service
metadata:
  name: fitness-service
spec:
  type: NodePort
  selector:
    app: fitness   # IMPORTANT: matches BOTH A and B
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fitness
      version: green
  template:
    metadata:
      labels:
        app: fitness
        version: green
    spec:
      containers:
        - name: app
          image: aceest-fitness:v2
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
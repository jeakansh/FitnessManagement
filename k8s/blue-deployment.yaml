apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fitness
      version: blue
  template:
    metadata:
      labels:
        app: fitness
        version: blue
    spec:
      containers:
        - name: app
          image: aceest-fitness:v1
          imagePullPolicy: Never
          ports:
            - containerPort: 5000